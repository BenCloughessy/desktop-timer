{
  "name": "timer",
  "version": "1.0.1",
  "description": "Desktop Timer with Electron",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "electron-packager . --overwrite --platform=win32 --arch=x64 --prune=true --out=release-builds"
  },
  "author": "Ben Cloughessy",
  "license": "ISC",
  "devDependencies": {
    "electron": "^23.1.4",
    "electron-packager": "^17.1.1"
  }
}
